# Backend Environment Variables (for deployment)

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Configuration - Add your frontend URLs here
# Format: comma-separated list of URLs
# Example for Netlify deployment:
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://your-app-name.netlify.app,https://*.netlify.app

# Model Settings (optional - defaults are fine)
# N_FACTORS=20
# N_ITERATIONS=60
# REG_LAMBDA=0.25
# LEARNING_RATE=0.005
# CONTENT_WEIGHT=0.15
# SEMANTIC_WEIGHT=0.20

# Note: Update the Netlify URL after deploying your frontend
# Example: https://movie-recommender-app.netlify.app
